# ignore every files in ./rom

rom/*

# ignore every file which ends in .scrap - used for my own scrap note purposes

*.scrap

# ignore every file in src/test but the .cpp files

src/test/*
!src/test/*.cpp
